#!/usr/bin/env ion

set -e

let server      = robi.private
let server_path = /srv/www/terranix
let syncfolder  = sync

echo
echo "create page"
echo
hugo

echo
echo "copy stuff"
echo
rsync \
  --recursive \
  --compress \
  --checksum \
  --verbose \
  --human-readable \
  --partial --progress \
  --protect-args \
  --delete-after \
  public/ $server:$server_path/



